import React from 'react';
import ReactDOM from 'react-dom';
import App from './components/App';

const domContainer = document.querySelector('#nps-passport');
ReactDOM.render(React.createElement(App), domContainer);
